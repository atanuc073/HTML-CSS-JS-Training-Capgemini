function display(){
    for(let idx=0 ; idx<5 ; ++idx){
        console.log(`Hello ${idx}`);
        
    }
    // console.log(`Hello${idx}`);
}
display();

// declear a variable using let it is block scope